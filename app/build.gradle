import java.util.Properties

plugins {
    id 'com.android.application'
    id 'org.jetbrains.kotlin.android'
    id 'org.jetbrains.kotlin.plugin.serialization'
    id 'org.jetbrains.kotlin.plugin.compose'     // ✅ ВАЖНО для Kotlin 2.0.x + Compose
    id 'com.google.devtools.ksp'
    id 'kotlin-parcelize'
}

def loadProps(String fileName) {
    def props = new Properties()
    def f = rootProject.file(fileName)
    if (f.exists()) {
        f.withInputStream { props.load(it) }
    }
    return props
}

def signingProps = loadProps("signing.properties")
def versionProps = loadProps("version.properties")

def major = (versionProps.getProperty("majorVersion", "1") as int)
def minor = (versionProps.getProperty("minorVersion", "0") as int)
def patch = (versionProps.getProperty("patchVersion", "0") as int)
def versionSuffix = versionProps.getProperty("versionSuffix", "")

android {
    namespace 'com.done.weather'
    compileSdk rootProject.ext.android_compile_sdk_version

    defaultConfig {
        applicationId "com.done.weather"
        minSdk rootProject.ext.android_min_sdk_version
        targetSdk rootProject.ext.android_target_sdk_version

        versionCode 10000 * major + 100 * minor + patch
        versionName "${major}.${minor}.${patch}"
        if (versionSuffix != null && !versionSuffix.trim().isEmpty()) {
            versionNameSuffix versionSuffix
        }

        testInstrumentationRunner "androidx.test.runner.AndroidJUnitRunner"
        vectorDrawables { useSupportLibrary true }

        buildConfigField("String", "BASE_URL", "\"https://ausapi.verifeye.info/\"")
    }

    signingConfigs {
        release {
            def keystoreFile = signingProps.getProperty("KEYSTORE_FILE", "")
            def keystorePassword = signingProps.getProperty("KEYSTORE_PASSWORD", "")
            def keyAliasValue = signingProps.getProperty("KEY_ALIAS", "")

            if (keystoreFile != null && !keystoreFile.trim().isEmpty()) {
                storeFile file(keystoreFile)
                storePassword keystorePassword
                keyAlias keyAliasValue
                keyPassword keystorePassword
            }
        }
    }

    buildTypes {
        release {
            minifyEnabled false
            shrinkResources false
            signingConfig signingConfigs.release
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
        debug {
            minifyEnabled false
            shrinkResources false
            versionNameSuffix "-debug"
            applicationIdSuffix ".debug"
        }
    }

    lint { disable 'ExpiredTargetSdkVersion' }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }

    kotlinOptions { jvmTarget = rootProject.ext.jvm_extension }

    buildFeatures {
        buildConfig true
        viewBinding true
        compose true
    }

    // ❌ НЕ НУЖНО при Kotlin 2.0.x + plugin.compose
    // composeOptions { kotlinCompilerExtensionVersion = ... }

    packaging {
        resources { excludes += ["/META-INF/{AL2.0,LGPL2.1}"] }
    }

    testOptions {
        unitTests.includeAndroidResources = true
        animationsDisabled = false
    }

    applicationVariants.all { variant ->
        variant.outputs.all { output ->
            outputFileName = "vgps_${variant.versionName}_${variant.versionCode}.apk"
        }
    }
}

// ✅ Room schema
ksp { arg("room.schemaLocation", "$projectDir/schemas") }

dependencies {
    implementation(project(":auth"))

    // AndroidX
    implementation rootProject.ext.androidx_core
    implementation rootProject.ext.androidx_core_ktx
    implementation rootProject.ext.androidx_appcompat
    implementation rootProject.ext.androidx_fragment
    implementation rootProject.ext.android_material
    implementation rootProject.ext.google_flexbox
    implementation rootProject.ext.play_services_location
    implementation rootProject.ext.androidx_browser
    implementation rootProject.ext.androidx_datastore
    implementation rootProject.ext.androidx_lifecycle_service
    implementation rootProject.ext.lifecycle_runtime_compose

    // Coroutines
    implementation rootProject.ext.kotlinx_coroutines_android

    // Room
    implementation rootProject.ext.room_runtime
    implementation rootProject.ext.room_ktx
    ksp rootProject.ext.room_compiler

    // Koin
    implementation platform("io.insert-koin:koin-bom:${rootProject.ext.koin_bom}")
    implementation "io.insert-koin:koin-android"
    implementation "io.insert-koin:koin-androidx-compose"

    // Compose (BOM)
    implementation platform(rootProject.ext.compose_bom)
    androidTestImplementation platform(rootProject.ext.compose_bom)
    implementation rootProject.ext.compose_ui
    implementation rootProject.ext.compose_tooling_preview
    implementation rootProject.ext.compose_material3
    debugImplementation rootProject.ext.compose_tooling
    debugImplementation rootProject.ext.compose_test_manifest
    implementation rootProject.ext.compose_navigation
    implementation rootProject.ext.compose_constraintlayout

    // ✅ Ktor (чтобы импорты Auth/Logging/ContentNegotiation работали)
    implementation rootProject.ext.ktor_client_core
    implementation rootProject.ext.ktor_client_okhttp
    implementation rootProject.ext.ktor_client_content_negotiation
    implementation rootProject.ext.ktor_serialization_json
    implementation rootProject.ext.ktor_client_logging
    implementation rootProject.ext.ktor_client_auth

    // ✅ kotlinx-datetime (Instant / Clock / LocalDate / todayIn)
    implementation rootProject.ext.kotlinx_datetime

    // CameraX / MediaPipe
    implementation rootProject.ext.camerax_core
    implementation rootProject.ext.camerax_camera2
    implementation rootProject.ext.camerax_lifecycle
    implementation rootProject.ext.camerax_view
    implementation rootProject.ext.mediapipe_tasks_vision

    // Utils
    implementation rootProject.ext.gson
    implementation rootProject.ext.coil
    implementation rootProject.ext.timber
    implementation rootProject.ext.picasso

    // Tests
    testImplementation rootProject.ext.junit_lib
    androidTestImplementation rootProject.ext.androidx_test_core_ktx
    androidTestImplementation rootProject.ext.androidx_test_uiautomator
    androidTestImplementation rootProject.ext.androidx_test_runner
    androidTestImplementation rootProject.ext.espresso_core
}
